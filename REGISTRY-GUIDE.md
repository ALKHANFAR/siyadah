# 📦 نظام إدارة سجل الأدوات — Siyadah

## الهيكل

```
data/registry/
├── pieces/                    ← 🔑 ملف مستقل لكل أداة (المصدر الحقيقي)
│   ├── google-sheets.json
│   ├── openai.json
│   ├── slack.json
│   └── ... (50 ملف)
├── tools.json                 ← 📦 السجل المُجمّع (لا تعدّله مباشرة!)
└── piece-schema.json          ← 📋 القالب والقواعد
```

```
scripts:
├── add-piece.py               ← ➕ أضف أداة جديدة
├── build-registry.py          ← 🏗️ ابنِ السجل + تحقق
└── tests/test-registry.js     ← ✅ اختبارات (52 اختبار)
```

---

## ➕ كيف أضيف أداة جديدة؟

### 3 خطوات فقط:

```bash
# 1️⃣ أنشئ قالب
python3 add-piece.py template asana

# 2️⃣ عبّي البيانات من المصدر الرسمي
#    روح: https://www.activepieces.com/pieces/asana
#    انسخ كل action name و trigger name بالحرف
#    عدّل الملف: data/registry/_drafts/asana.json

# 3️⃣ احفظ + ابنِ + اختبر
python3 add-piece.py save asana
python3 build-registry.py
node tests/test-registry.js
```

### ⚠️ قواعد النسخ:
- **انسخ الاسم بالحرف** من الموقع الرسمي — لا تترجم ولا تعدّل
- مثال: `sendMessage` ✅ مش `send_message` ❌
- مثال: `create_google_calendar_event` ✅ مش `create_event` ❌
- trigger type: `instant` (فوري/webhook) أو `scheduled` (جدولة/polling)

---

## 🔄 كيف أحدّث أداة موجودة؟

```bash
# عدّل الملف مباشرة
nano data/registry/pieces/openai.json

# ابنِ واختبر
python3 build-registry.py
node tests/test-registry.js
```

---

## 🔄 كيف أتعامل مع تحديث ActivePieces؟

### عند نزول إصدار جديد:

```bash
# 1. شوف التغييرات
# https://github.com/activepieces/activepieces/releases

# 2. لكل أداة تحدثت:
#    روح https://www.activepieces.com/pieces/{name}
#    حدّث الملف في data/registry/pieces/{name}.json

# 3. ابنِ واختبر
python3 build-registry.py
node tests/test-registry.js
```

---

## الحماية من الأخطاء

### البناء يرفض تلقائياً:
- ❌ أسماء مكررة
- ❌ ملفات JSON فاسدة
- ❌ حقول مطلوبة ناقصة
- ❌ أداة في flow غير موجودة في السجل
- ❌ category أو auth_type غير صالح

### الاختبارات تتحقق من:
- ✅ كل أداة فيها كل الحقول
- ✅ الأسماء المخترعة حُذفت نهائياً
- ✅ الأسماء الرسمية صحيحة (17 أداة متحققة بالحرف)
- ✅ كل tool_id في الـ Flows موجود في السجل
- ✅ اسم الملف = ID.json
- ✅ عدد الملفات = عدد الأدوات

---

## 📊 الحالة الحالية

| المقياس | القيمة |
|---------|--------|
| أدوات | 50 |
| Actions | 168 |
| Triggers | 72 |
| متحقق من المصدر | 17 |
| غير متحقق | 33 |
| اختبارات | 52/52 ✅ |
| Flows متوافقة | 6/6 ✅ |
