# سيادة Siyadah — ملف الانتقال الكامل
# Handoff Document — For New Claude Conversation
# التاريخ: 28 فبراير 2026

---

## 🔴 تعليمات لكلود الجديد:

أنت تكمّل مشروع سيادة (Siyadah). اقرأ هذا الملف بالكامل قبل ما تسوي أي شي.
- الملفات اللي بنيناها موجودة في ملف `siyadah-phase1-2-3-complete.tar.gz` المرفق
- فك الضغط أول: `tar xzf siyadah-phase1-2-3-complete.tar.gz`
- المرجع الرئيسي: ملف `siyadah-master-reference-v3.docx` أو PDF المرفق (siyadahmasterreferencev3_docx.pdf)
- **لا تعيد بناء المراحل 1-3** — جاهزة ومختبرة

---

## 1. وش هو سيادة؟

منصة أتمتة عربية SaaS. المدير يكلّم شات عربي → سيادة تبني أتمتة كاملة.
- المحرك: ActivePieces (مخفي عن العميل)
- المبدأ الأساسي: "انسخ لا تخمّن" — AI ينسخ من قوالب مجرّبة، ما يخترع
- الجمهور: مدراء خليجيين (5-20 موظف)
- 7 صناعات: عيادة، متجر، مطعم، استشارات، بناء، تدريب، خدمات عامة

---

## 2. الخطة: 14 مرحلة

```
المجموعة 1 — البيانات:
  ① Tool Registry     ✅ تم (44/44 اختبار)
  ② Tool Details      ✅ تم (26/26 اختبار)
  ③ Flow Templates    ✅ تم (51/51 اختبار)
  ④ Templates & Vars + Prompt Library  ⏳ التالي
  ⑤ Error Map

المجموعة 2 — المحرك:
  ⑥ Tool Selector + Intent Detection
  ⑦ Flow Builder
  ⑧ Validator (5 Safety Gates)
  ⑨ Full Pipeline

  9.5 AP API + Security + Infrastructure (مرحلة جديدة)

المجموعة 3 — SaaS:
  ⑩ Auth + Multi-Tenancy + DB Schema
  ⑪ Billing + Usage + Lifecycle

  11.5 DB Schema Detail + DevOps + Load Test (مرحلة جديدة)

المجموعة 4 — الواجهة:
  ⑫ Full UI + Integration
```

**القاعدة**: لا مرحلة تبدأ إلا لما اللي قبلها تنجح 100%

---

## 3. وش بنينا بالضبط (المراحل 1-3)

### المرحلة 1: Tool Registry ✅
- **الملف**: `data/registry/tools.json`
- **المحتوى**: 50 أداة مسجّلة (15 أساسية + 15 مهمة + 10 متقدمة + 10 بريميوم)
- **لكل أداة**: id, displayName, package, version, category, auth, reliability_score, fallbacks
- **الاختبارات**: `tests/test-registry.js` — 44/44 ✅
- **الحجم**: 41KB

### المرحلة 2: Tool Details ✅
- **الملفات**: `data/tools/*.json` (30 ملف)
- **المحتوى**: تفاصيل كل أداة — actions, triggers, input_fields, output_fields, errors, auth_config
- **الإحصائيات**: 73 action, 33 trigger, 72 error مع auto-fix
- **الاختبارات**: `tests/test-tool-details.js` — 26/26 ✅
- **الحجم**: 97KB

### المرحلة 3: Flow Templates ✅
- **الملفات**: `data/flows/*.json` (6 ملفات)
- **القوالب الستة**:

```
1. lead-capture.json
   - Trigger: webhook → Format phone → Save sheet → AI classify → Branch (hot/warm/cold) → Welcome → Notify
   - 7 خطوات + تفرع ثلاثي

2. invoice-collection.json
   - Trigger: schedule (9am daily) → Fetch overdue → Loop → Format phone → Calculate days → Send reminder (4 levels) → AI summary → Notify
   - 9 خطوات + loop + تصعيد

3. customer-journey.json
   - Trigger: new row → Check duplicate → Format phone → Welcome → Delay 3 days → Follow-up 1 → Delay 4 days → Follow-up 2 → Delay 7 days → Review request → Update complete
   - 11 خطوة + 3 تأخيرات (14 يوم)

4. weekly-report.json
   - Trigger: schedule (Sunday 9am) → Fetch leads → Fetch invoices → Calculate stats → Format phone → AI summary → Send report
   - 6 خطوات

5. appointment-booking.json
   - Trigger: google-calendar new event → Extract info → Save sheet → Confirm customer → Notify team → Delay → Day-before reminder → Delay → Hour-before reminder
   - 8 خطوات + 2 تذكير

6. complaint-handling.json
   - Trigger: webhook → Format phone → AI classify → Save sheet → Acknowledge customer → Notify team → Branch (high severity → escalate)
   - 5 خطوات + تصعيد
```

- **كل قالب فيه**: _meta, trigger + alternatives, steps مع on_error, branches, protection (idempotency + rate_limit + circuit_breaker), industry_variants, sheets_template عربي, success_criteria (must/should/nice)
- **الاختبارات**: `tests/test-flows.js` — 51/51 ✅
- **الحجم**: 61KB

---

## 4. القرارات والمبادئ اللي اتفقنا عليها

### مبدأ "انسخ لا تخمّن"
- كل أتمتة تُبنى بنسخ قالب جاهز + حقن متغيرات
- AI لا يخترع flows من راسه أبداً

### Adapter Layer (قرار مهم)
- بنيت الأسماء والحقول من معرفة عامة بـ ActivePieces
- **ما تأكدنا من السورس كود الفعلي** (الشبكة كانت مقفلة)
- الحل المتفق عليه: لما نوصل المرحلة 9 نبني "طبقة ترجمة" (Adapter)
- المنطق والتسلسل ما يتغير — بس الأسماء والصيغ ممكن تتعدّل
- **المستخدم واعي لهالنقطة ومتفق عليها**

### طريقة العمل
```
المستخدم يقول: "يلله المرحلة X"
← كلود يبني الملفات + يكتب الاختبارات + يشغّلها
← كلود يصلّح أي أخطاء داخلياً
← كلود يعرض: "Phase X: XX/XX passed ✅"
← المستخدم يقول: "كمل" → المرحلة اللي بعدها
```

### رفع GitHub
- مرة بعد المرحلة 9 (المحرك كامل)
- مرة بعد المرحلة 12 (كل شي كامل)
- الريبو: ALKHANFAR/autoflow2

### الاختبارات
- 4 طبقات: Unit → Integration → E2E Simulation → Real Deploy
- المراحل 1-5: اختبارات بنية وتناسق
- المرحلة 9+: اختبارات حقيقية مع AP
- **المستخدم يبي اختبارات صارمة لكن عملية**

---

## 5. المرحلة التالية: ④ Templates & Vars + Prompt Library

### المطلوب حسب Master Reference:
```
- data/templates/messages/ — رسائل لـ 7 صناعات (28+ رسالة)
  - كل صناعة: welcome, follow_up, reminder, complaint_ack
  
- data/templates/sheets/ — أعمدة الشيتات لكل صناعة

- data/variables/ — 4 ملفات متغيرات:
  - company.json — متغيرات الشركة
  - customer.json — متغيرات العميل
  - context.json — متغيرات محسوبة (تحية، يوم، وقت)
  - ai.json — متغيرات AI

- AI Prompts Library — بروبتات جاهزة لكل مهمة AI:
  - تصنيف عملاء
  - تصنيف شكاوى
  - ملخص أسبوعي
  - رد تلقائي
```

### معيار النجاح:
```
28+ messages + 4 var files + prompts
7 صناعات، لا متغيرات يتيمة، اختبار حقن، لهجة سعودية
```

---

## 6. البنية الحالية للملفات

```
siyadah/
├─ data/
│  ├─ registry/tools.json          ← 50 أداة (المرحلة 1)
│  ├─ tools/*.json (×30)           ← تفاصيل الأدوات (المرحلة 2)
│  ├─ flows/*.json (×6)            ← قوالب الأتمتة (المرحلة 3)
│  ├─ templates/messages/          ← فاضي (المرحلة 4)
│  ├─ templates/sheets/            ← فاضي (المرحلة 4)
│  ├─ variables/                   ← فاضي (المرحلة 4)
│  └─ errors/                      ← فاضي (المرحلة 5)
├─ tests/
│  ├─ test-registry.js             ← 44 اختبار ✅
│  ├─ test-tool-details.js         ← 26 اختبار ✅
│  └─ test-flows.js                ← 51 اختبار ✅
└─ generate-tools.js               ← سكربت توليد أدوات
```

---

## 7. إحصائيات مجمّعة

```
إجمالي الاختبارات: 121/121 ✅
إجمالي الملفات: 37+ ملف JSON
إجمالي الحجم: ~205KB بيانات
الأدوات: 50 (مع fallbacks)
الأكشنات: 73 action + 33 trigger
الأخطاء المعرّفة: 72 (مع auto-fix)
القوالب: 6 flows (46 خطوة، 3 تفرعات، 15 fallback chain)
الصناعات: 7
```

---

## 8. ملاحظات مهمة للمحادثة الجديدة

1. **المستخدم يتكلم عربي سعودي** — رد بالعربي السعودي
2. **المستخدم يحب الشفافية** — قل له وش تقدر ووش ما تقدر بصراحة
3. **المستخدم سأل عن مصداقية الاختبارات** — واعترفنا إنها اختبارات تناسق مو واقعية، والواقعية تجي المرحلة 9
4. **المستخدم يبي يعرف "ليش"** — لا تسوي شي بدون ما تشرح السبب
5. **لا تعيد بناء شي موجود** — فك الضغط واستخدم الملفات الجاهزة
6. **اسم المشروع القديم**: AutoFlow → تغيّر لـ سيادة (Siyadah)

---

## 9. كيف تبدأ المحادثة الجديدة

الكلام الأول للمستخدم:
```
"يلله المرحلة 4"
أو
"كمّل من وين وقفنا"
```

أول شي يسويه كلود:
```
1. يفك ضغط الملفات: tar xzf siyadah-phase1-2-3-complete.tar.gz
2. يتأكد الملفات سليمة: node tests/test-registry.js && node tests/test-tool-details.js && node tests/test-flows.js
3. يبدأ المرحلة 4
```

---

نهاية ملف الانتقال — سيادة v3.0
